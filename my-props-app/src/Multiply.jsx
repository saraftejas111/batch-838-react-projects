import React from 'react'

const Multiply = (props) => {
    return (
        <div>
            <h2>
                Multiply = {props.x * props.y}
            </h2>
        </div>
    )
}

export default Multiply
